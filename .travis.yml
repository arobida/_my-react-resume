language: node_js
node_js:
  - 'stable'
install:
  - npm install
cache:
  directories:
    - node_modules
script:
  - npm test
  - npm run build
deploy:
  provider: netlify
  skip_cleanup: true
  domain: https://arobida.com
  project: ./build/
on:
  branch: master
